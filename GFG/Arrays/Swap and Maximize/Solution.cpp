#include <bits/stdc++.h>
#include <cstdlib>

using namespace std;

long long int maxSum(int a[], int n) {
     
}

int main() {

    int n;
    cin >> n;
    int a[n];
    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }

    cout << maxSum(a, n) << endl;

    return 0;
}